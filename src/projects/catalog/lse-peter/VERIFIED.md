# LSE Peter — silhouette check

| Feature | Target | Delivered by |
|---|---|---|
| Peter silhouette | Shared rounded torso, sphere head, stub arms | `primitives.peterAvatar` — identical across all 6 Peters |
| LSE brand body | Deep purple | body = `#6b1f5f` (LSE's official purple) |
| Academic emblem | Stack of 3 books with gilt trim, floating above head | emblemBuilder returns 3 BoxGeometry books descending in size, each with a thin gilt plate on the top edge |
| Skin tone | Neutral | `#e8c4a2` — shared across all Peters so the network reads as a family |

No ability.
